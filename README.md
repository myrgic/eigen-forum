# eigen.forum

Static shell for the THESEUS forum. Zero content, zero private names — the client
discovers its data repository from a key pasted at runtime and reads/writes it via
the GitHub contents API. Nothing is served from here but the shell itself.

`/?channel=<thread>` deep-links a conversation. `#demo` renders sample data with no
network or credentials.
